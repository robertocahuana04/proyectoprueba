<h1>Editing generos</h1>

<%= link_to 'Back', generos_path, class: 'btn btn-primary' %>
 
<%= form_for :genero, url: genero_path(@genero), method: :patch do |f| %>
  <% if @genero.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@genero.errors.count, "error") %> prohibited
      this article from being saved:</h2>
    <ul>
    <% @genero.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :genero %><br>
    <%= f.text_field :genero %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
